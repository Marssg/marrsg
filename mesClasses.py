class User(object): # Définition de la classe utilisateur
    """
    Classe définissant un utilisateur et ses attributs
    - nom = user name (type = str)
    - wishlist = what this user would like (type = WishList)
    - no_wishlist = what this user doesn't want (type = WishList)
    """
   
    def __init__(self):
        """on défini les valeurs par défaut des attributs"""
        self.nom = nom
